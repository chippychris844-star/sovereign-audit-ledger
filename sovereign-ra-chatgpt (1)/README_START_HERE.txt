RESOLUTION AI - START HERE

1. Extract the ZIP to a normal folder, for example D:\SovereignRA\ResolutionAI or Documents\ResolutionAI.
2. Double-click: CREATE DESKTOP ICON - RESOLUTION AI.cmd
3. After that, use the Resolution AI icon on your Desktop.

Windows warning note:
This package is created in ChatGPT and is not code-signed by Microsoft or an installer vendor. Windows may warn about any downloaded .bat/.cmd file even when it is local-only. The launcher does not request administrator permission. It starts a local Node.js server at http://localhost:3030 and stores private data under D:\SovereignRA when D: exists.

Operational defaults:
- Claude / Anthropic first when a valid key is found.
- Ollama local fallback.
- OpenAI is advanced/manual only.
- D-drive private storage is preferred.
- Attachments and voice input are enabled in the interface.
- Local file access is opt-in and uses approved folders.
- Every answer returns a verification badge.
